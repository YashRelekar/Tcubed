# ported/2002/

This directory is reserved for files ported from
[`Phatshark05/2002`](https://github.com/Phatshark05/2002) (repo ID `1219671575`)
that would conflict with existing paths in `YashRelekar/Tcubed`.

## Why this directory is currently empty

The source repository is **private** and the Copilot coding agent was not granted
access to it when this PR was created (2026-04-30).  See [`PORTED_FROM.md`](../../PORTED_FROM.md)
at the repository root for details and instructions on how to complete the port.

## Directory layout (once populated)

Once the porting is complete, files are placed here preserving the exact relative
paths they had in `Phatshark05/2002`.  For example, a conflicting file at `README.md`
in the source repo would appear here as `ported/2002/README.md`.
